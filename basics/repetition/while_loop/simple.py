# This task is weird, much simpler way to do this

toRemove = int(input("How many cables should I remove? "))
removedCables = 0

while (removedCables < toRemove):
	print("Removed cable.")
	removedCables += 1
